import { FC } from 'react';
import { HeroSwiper } from './HeroSwiper';

const Hero: FC = () => {
  return (
    <section className="h-60">
      <HeroSwiper />
    </section>
  );
};

export { Hero };
